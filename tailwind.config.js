/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./src/**/*.js"], // Ensures Tailwind scans all JS files in the src directory
  theme: {
    extend: {
      // You can extend the default theme here
    },
  },
  plugins: [],
}
